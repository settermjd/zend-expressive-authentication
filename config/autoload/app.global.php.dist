<?php

/**
 * This is a sample configuration file which invokes the ConfigProvider class
 * for the App namespace. To use it, copy it from here to your application's
 * `config/autoload` directory, renaming it to `app.global.php`. To learn more
 * about ConfigProvider classes, check out:
 * http://www.masterzendframework.com/configproviders-classes/.
 */
use App\ConfigProvider;

return (new ConfigProvider())->__invoke();
