<?php

/**
 * This is a sample configuration file which invokes the ConfigProvider class
 * for Zend\Form. To use it, copy it from here to your application's
 * `config/autoload` directory, renaming it to `forms.global.php`.
 */
use Zend\Form\ConfigProvider;

return (new ConfigProvider())->__invoke();